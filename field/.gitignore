node_modules
dist
data.db
data.db-journal
data.db-shm
data.db-wal
.env
.env.local
*.log

# Raw Compass seed JSONs are private — keep them out of git by default.
# Curated public seeds under seeds/public/ are explicitly allowed so the
# Railway image can ship the three beta Living Profiles deterministically.
seeds/*.json
!seeds/README.md
!seeds/public/
